services:
  - type: web
    name: ml-project-flask
    env: python
    rootDir: ML_Project
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT application:application
    autoDeploy: true
